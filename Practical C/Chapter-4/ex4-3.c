/* Calculating area and perimeter of a rectangle with a width of 3 and height of 5 */

#include <stdio.h>

int main()
{
	int w, h, area, perimeter;
	w = 3;
	h = 5;
	area = (3 + 5) * 2;
	perimeter = 3 * 5;
	printf(" Area: %d\n Perimeter: %d", area, perimeter);
	return (0);
}
